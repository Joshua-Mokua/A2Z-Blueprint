# Virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# Streamlit secrets (never commit)
.streamlit/secrets.toml

# CBS / bank data (sensitive raw data)
cbs_data/
data/actuals_*.xlsx
data/actuals_*.csv
data/A2Z*.xlsx

# Excel uploads
*.xls

# Backup files
*_backup.py

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs
*.log

# Node / React (pnpm workspace)
node_modules/
dist/
build/
.env
.env.local
.env.*.local
*.tsbuildinfo
coverage/
.npm/
.pnpm-store/

# Auth seeds (plaintext credentials — replaced by hashed users in v10.497)
data/users.json
data/jwt_blocklist.json

# Test artifacts (curl cookie jars, login responses)
cookies.txt
# Audit logs (runtime telemetry, regenerated by server)
data/audit_log.json
data/audit_trail.jsonl
*_response.json
# Versioned in-repo backup snapshots (use git history instead)
data/_v*_backups/
# Versioned in-repo backup snapshots (use git history instead)
data/_v*_backups/
# Stale nested project copy (pre-v10.22, removed in cleanup before v10.497)
a2z/
